package se.lexicon.garage;

import org.junit.jupiter.api.Test;

import java.util.ArrayList;

import static org.junit.jupiter.api.Assertions.*;

class MySQLConnectionTest {

   /* @Test
    void saveVehicleDB() {
        MySQLConnection.saveVehicleDB(new Car("Ford", 220));
        ArrayList<Vehicle> vehicles = MySQLConnection.getVehiclesDB();
        Car car = (Car)vehicles.get(vehicles.size()-1);
        assertEquals("Ford", car.getMake());
        assertEquals(220, car.getTopSpeed());
    }*/
}